
  # ShelterConnect Mobile PWA (Copy)

  This is a code bundle for ShelterConnect Mobile PWA (Copy). The original project is available at https://www.figma.com/design/cWBT3gtpnh1HlJZk9oNOeF/ShelterConnect-Mobile-PWA--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  